# CONTRATO DO REPO — q1jobs

- Fonte da verdade: o repo e o stdout do comando. Não o treino do modelo.
- Não invente arquivo, pacote, endpoint, env, tabela ou SDK. Sem evidência: `NÃO CONFIRMADO`.
- Antes de editar: liste arquivos que vai tocar e o que não vai tocar.
- Uma fatia por sessão. Sem "já deixo o resto pronto".
- Pronto = comando de verificação rodado e stdout colado.
- Stack = a que já existe neste repo (JS + server em `src/`). Não troque framework sem pedido explícito.
- Providers novos só depois do contrato canônico e do dedup (fatias 2 e 3).
- Clinic, agents e billing compartilhado não entram neste repo.
- Se faltar dado, pergunte. Não assuma produto, preço, auth ou schema.
